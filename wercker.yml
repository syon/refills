box: nodesource/trusty

build:
  steps:
    - npm-install
    - script:
        name: Metalsmith build
        code: npm run build

deploy:
  steps:
    - lukevivier/gh-pages:
        token: $GH_TOKEN
        basedir: refills
