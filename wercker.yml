box: nodesource/trusty

build:
  steps:
    - npm-install

deploy:
  steps:
    - script:
        name: Metalsmith build
        code: npm run build

    - lukevivier/gh-pages:
        token: $GH_TOKEN
        basedir: refills
