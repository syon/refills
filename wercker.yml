box: nodesource/trusty

build:
  steps:
    - npm-install

deploy:
  steps:
    - script:
        name: npm link
        code: npm link

    - script:
        name: Metalsmith build
        code: npm run build

    - lukevivier/gh-pages:
        token: $GH_TOKEN
        basedir: refills
